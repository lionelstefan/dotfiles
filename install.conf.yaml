- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: .zshrc
    ~/.config/nvim/init.vim: nvim/init.vim
- shell:
  - [git submodule update --init --recursive, Installing submodules]
